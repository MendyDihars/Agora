<%= simple_form_for Meeting.new, url: teacher_meetings_path(@teacher), method: :post do |f| %>
  <%= f.input :skill_id, collection: @teacher.own_skills  %>
  <%= f.input :happen_at %>
  <%= f.input :message  %>
  <%= f.submit %>
<% end %>
