 <%= simple_form_for Review.new, url: teacher_reviews_path(@teacher), method: :post do |f| %>
      <%= f.input :content %>
      <%= f.input :rating, collection: (1..5).to_a, default: 1 %>
      <%= f.submit "add a review", class: "btn btn-primary" %>
    <% end %>
